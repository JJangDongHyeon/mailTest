<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.projectTest.user.UserMapper">

    <insert id="postUser"
            useGeneratedKeys="true"
            keyProperty="userId">
        INSERT INTO user
        SET email = #{email},
        upw = #{upw},
        nickname  = #{nickname}

    </insert>


    <select id="getUserById">
        SELECT user_id AS userId, email, upw, nickname, created_at AS createdAt, profile_update AS profileUpdate
        FROM user
        WHERE email = #{email}
    </select>


</mapper>